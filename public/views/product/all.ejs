<html>
    <head>
        <title>All Products</title>
    </head>
    <body>
        <h1><u>Product List</u></h1>
        <% console.log(products) %>
          <% for(var i=0; i<products.length; i++) { %>
          <h1><%=(i+1)%>) <a href="/products/<%=products[i]._id %>">  
            <%= products[i].name %> </a> </h1>
            
            <% } %>
    </body>
</html>